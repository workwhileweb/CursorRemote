https://github.com/len5ky/cursorremote


hiện đã có cursor agents work remote, ứng dụng này vô dụng
nhưng có thể dùng kỹ thuật này để làm việc ứng dụng khác


-thêm public link bằng clloudflare và QR code là 2fa được lưu, show link public và qr


# inject vào cursor yếu lắm vì không capture hết được
và không tức thời
tốt nhất là chạy remote bằng máy tính bảng còn hơn
khi xài phone chỉ nên chat



start relay đã mở đúng curssor với thư mục chỉ định, tuy nhiên port gắn cho cursor không work : http://localhost:9222/json error, trên relay status waiting for cursor, có thể là do cursor không thể create new instance với debug port, khi có instance cũ đang chạy, nếu vậy thì multi debug port cũng không hoạt động. hãy thử tìm cách cô lập tiến tình (change env ...) hoặc create new hardlink of cursor folder để change path, hãy tự test xem có work không. btw: khi click start relay trong launcher, nếu đã có relay hoạt động thì phải open lại relay đó 



Thêm --user-data-dir riêng mỗi session (cách VS Code/Electron tách instance) và chờ CDP sẵn sàng trước khi bật relay. Nếu relay đã chạy và CDP phản hồi, chỉ trả về URL (mở lại) thay vì khởi động lại.
mọi thứ có vẻ đã chạy, tuy nhiên vì user-data-dir mới nên cursor trống và chưa đăng nhập, thay vì tạo mới folder user-data-dir hãy create 1 junction hardlink qua user-data-dir cũ global của cursor xem thế nào


Lưu ý: Hai tiến trình Cursor cùng lúc trên cùng nội dung user-data vẫn có thể gặp lock / single-instance của Electron; nếu gặp, thử chỉ mở một instance hoặc bật CURSOR_REMOTE_PROFILE_ISOLATED=1 cho session đó.








new session chỉ tạo mới 1 folder trong data và call start relay với tham số là thư mục đó,
start relay kiểm tra thư mục đó và :
-nếu có file lock cũ thì check port và pid còn tồn tại không, nếu còn tồn tại thì kill cursor gắn với port đó (hoặc kill processid)
-check free port mới và launch new cursor với debug port mới
-cập nhật pid và port mới vào lock
-show ui relay

gom laucher về giao diện relay và chayj trong @src/server/index.ts  luôn, thêm 1 button trong relay show modal dialog show launcher và switch relay, nếu chạy lần đầu thì show launcher luôn
sẽ không còn pnpm run launcher nữa chỉ còn pnpm run dev


lỗi : khi mở http://127.0.0.1:3000/  show launcher, bấm start relay hiện thị text : Relay: http://127.0.0.1:3001
vào url http://127.0.0.1:3001/ thì thấy Cannot GET /launcher. cursor vẫn chưa mở, và toàn bộ ui nên nằm chung 1 port, không mở thêm port, bấm start relay thì chuyển qua giao diện ui của relay đó ngay, ở trong giao diện ui của relay có button show launcher modal dialog để switch qua relay khác hoặc new relay


thêm 1 launcher với tính năng tạo session và quản lý session cursor làm việc,
pnpm run launcher khởi chạy sẽ show 1 form các phiên cursor đang mở,
và thêm mới 1 session mới với debug port khác,
tên phiên làm việc mới đặt theo Mnemonic Phrase ngắn dễ đọc, hoặc english dict,
phiên làm việc nằm trong thư mục ./data/[name]/.
mở cursor với debug port free và workking folder là ./data/[name]/,
lưu pid và port và .lock file trong thư mục làm việc,
tìm các phiên làm việc trước bằng cách quét thư mục con ./data lấy lock file lấy pid và port,
scan pid còn chạy không và port còn mở localhost:[port]/json không?
nếu không thì xóa lock file và thư mục đó đang không có session làm việc [hiển thị session inactive],
nếu có thì hiện thị session active
(ghi chú nếu pid và port 1 trong 2 cái mở mà cái kia tắt thì session
invalid và cần kill pid hoặc tìm cursor tương ứng với port đang mở để kill).
khi click vào 1 session thì nếu inactive thì call server index lên với tham số là folder và port,
chỉnh sửa lại code index để nhận tham số không fix cứng,
server index có thể close (close luôn cursor và pid) và có thể chạy lại bằng kết nối vào port cũ























"Hãy phân tích toàn bộ repository này từ 3 góc độ:
Kiến trúc sư phần mềm: Tập trung vào thiết kế hệ thống, các pattern (mô hình) kiến trúc và khả năng mở rộng.
Lập trình viên: Kiểm tra cấu trúc code, chi tiết triển khai, các thư viện sử dụng và khả năng bảo trì.
Quản lý sản phẩm: Đánh giá các tính năng chính, luồng người dùng (user flows) và mục đích của dự án.
Sau đó, hãy tổng hợp thành một file OVERVIEW.md có sử dụng sơ đồ Mermaid để giải thích luồng hoạt động."


"Tôi là thành viên mới của dự án này. Hãy giúp tôi:
Tóm tắt chức năng chính của repo này là gì?
Cấu trúc thư mục được tổ chức như thế nào? Giải thích ý nghĩa của các thư mục quan trọng.
Luồng dữ liệu chính đi từ đâu đến đâu? (Ví dụ: Từ API endpoint nào đến logic xử lý nào).
Liệt kê các công nghệ, framework và thư viện cốt lõi được sử dụng.
Sau đó, hãy tổng hợp thành một file .md có sử dụng sơ đồ Mermaid để giải thích luồng hoạt động.
"



"Hãy thực hiện một đợt review code toàn diện cho repo này. Tập trung vào:
Code Quality: Các đoạn code bị lặp lại (DRY), các hàm quá phức tạp cần refactor.
Security: Tìm kiếm các lỗ hổng bảo mật tiềm ẩn như SQL injection, lộ thông tin nhạy cảm hoặc lỗi phân quyền.
Performance: Chỉ ra các điểm thắt nút cổ chai (bottlenecks) về hiệu năng.
Best Practices: Code đã tuân thủ đúng quy chuẩn của ngôn ngữ [Tên ngôn ngữ] chưa?
Sau đó, hãy tổng hợp thành một file .md có sử dụng sơ đồ Mermaid để giải thích.
"



"Hãy tìm và giải thích chi tiết logic của tính năng :

quá trình flow step by step thực hiện faceswap chi tiết 

. Chỉ ra chính xác các file và hàm liên quan đến luồng này.
Sau đó, hãy tổng hợp thành một file .md có sử dụng sơ đồ Mermaid để giải thích luồng hoạt động
"
